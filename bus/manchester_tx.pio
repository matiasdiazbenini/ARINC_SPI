.program manchester_tx

.side_set 2 opt

.wrap_target
    pull block
    set y, 7

bitloop:
    out x, 1
    jmp !x bit_zero

bit_one:
    nop side 1 [3]
    nop side 2 [3]
    jmp next_bit

bit_zero:
    nop side 2 [3]
    nop side 1 [3]

next_bit:
    jmp y-- bitloop
.wrap